<form method="post" action="{{ path('personne_confiance_delete', {'id': personne_confiance.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ personne_confiance.id) }}">
    <button class="btn">Delete</button>
</form>
