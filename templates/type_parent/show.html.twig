{% extends 'base.html.twig' %}

{% block title %}TypeParent{% endblock %}

{% block body %}
    <h1>TypeParent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ type_parent.id }}</td>
            </tr>
            <tr>
                <th>LibelleParent</th>
                <td>{{ type_parent.libelleParent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('type_parent_index') }}">back to list</a>

    <a href="{{ path('type_parent_edit', {'id': type_parent.id}) }}">edit</a>

    {{ include('type_parent/_delete_form.html.twig') }}
{% endblock %}
