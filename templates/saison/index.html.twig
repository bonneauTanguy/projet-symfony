{% extends 'base.html.twig' %}

{% block title %}Saison index{% endblock %}

{% block body %}
    <h1>Saison index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>DateSaisonD</th>
                <th>DateSaisonF</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for saison in saisons %}
            <tr>
                <td>{{ saison.id }}</td>
                <td>{{ saison.dateSaisonD ? saison.dateSaisonD|date('Y-m-d') : '' }}</td>
                <td>{{ saison.dateSaisonF ? saison.dateSaisonF|date('Y-m-d') : '' }}</td>
                <td>
                    <a href="{{ path('saison_show', {'id': saison.id}) }}">show</a>
                    <a href="{{ path('saison_edit', {'id': saison.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('saison_new') }}">Create new</a>
{% endblock %}
