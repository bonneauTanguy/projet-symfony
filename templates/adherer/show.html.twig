{% extends 'base.html.twig' %}

{% block title %}Adherer{% endblock %}

{% block body %}
    <h1>Adherer</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ adherer.id }}</td>
            </tr>
			<tr>
                <th>Adherent</th>
                <td>{{ adherer.unAdherent }}</td>
            </tr>
			<tr>
                <th>Saison</th>
                <td>{{ adherer.uneSaison }}</td>
            </tr>
			<tr>
                <th>Ceinture</th>
                <td>{{ adherer.uneCeinture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('adherer_index') }}">back to list</a>

    <a href="{{ path('adherer_edit', {'id': adherer.id}) }}">edit</a>

    {{ include('adherer/_delete_form.html.twig') }}
{% endblock %}
