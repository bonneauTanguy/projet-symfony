{% extends 'base.html.twig' %}

{% block title %}Telephone{% endblock %}

{% block body %}
    <h1>Telephone</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ telephone.id }}</td>
            </tr>
            <tr>
                <th>Numero</th>
                <td>{{ telephone.numero }}</td>
            </tr>
			<tr>
				<th>type personne confiance</th>
				<td>{{ telephone.getUnTypeParent() }}</td>
			</tr>
        </tbody>
    </table>

    <a href="{{ path('telephone_index') }}">back to list</a>

    <a href="{{ path('telephone_edit', {'id': telephone.id}) }}">edit</a>

    {{ include('telephone/_delete_form.html.twig') }}
{% endblock %}
